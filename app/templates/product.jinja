{% extends 'index.html.jinja' %}

{% block title %}Home{% endblock %}

{% block content %}
    <h1>{{data ['title']}}</h1>
    <p>{{ data['description'] }}</p>
    <p>{{ data['price'] }}</p>
    {# <h1>Products</h1> #}
{% endblock %}
